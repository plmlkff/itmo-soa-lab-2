server:
  port: 8082
  error:
    include-message: always
services:
  product:
    baseUrl: "http://localhost:8081"
springdoc:
  api-docs:
    path: '/v3/api-docs'
    enabled: true
  swagger-ui:
    path: '/swagger-ui.html'
    enabled: true